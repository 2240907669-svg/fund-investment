# 催化信息库使用说明

`catalyst-ledger.csv` 是午报与晚报共用的可审计催化底稿。每条信息必须区分：

1. 事实是否由一手来源确认；
2. 相对市场预期是否真正新增；
3. 消息首次发布时间在收盘前还是收盘后；
4. 从事件到政策、订单、利润和基金净值的传导链；
5. 价格、成交、资金流与市场宽度是否确认；
6. 哪些证据会使当前判断失效。

日报运行时先读取全部 `scheduled` 和 `watch` 条目，再扫描 `config/catalyst-source-watchlist.json` 中的来源。事件发生后更新验证结果和状态，不用事后信息改写原始判断。
